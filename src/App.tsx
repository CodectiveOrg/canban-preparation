import { type ReactNode } from "react";

import "./App.css";

export default function App(): ReactNode {
  return (
    <div className="app">
      <header>Header</header>
      <main>Hello, friend!</main>
      <footer>Footer</footer>
    </div>
  );
}
